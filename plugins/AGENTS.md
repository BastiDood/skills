# Plugin Development Workflow

Whenever you complete a feature, bug fix, or refactor, you must bump the `version` of the plugin in its respective manifest file (i.e., `.{claude,cursor}-plugin/plugin.json`).
